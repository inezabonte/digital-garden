# Learning 📚

This section is dedicated to learning resources I come across.

import {getAllLearningBookmarks} from "../../lib/api.js"
import { useSSG } from "nextra/ssg";
import {HiExternalLink} from 'react-icons/hi'

export const getStaticProps = async ({params}) => {
    const data = await getAllLearningBookmarks()
    return {
        props: {
            ssg: {
                data
            }
        }
    }

}

export const Learning = () => {
    const {data} = useSSG()
    return (
        <div className="space-y-8 mt-8">
            {
                data.map((item) => (
                <div>
                    <h3 className="m-0">{item.name}</h3>
                    <span className="text-gray-500">{item.description}</span>
                    <a href={item.link} className="flex items-center" target="_blank">
                        <span>Link</span> 
                        <HiExternalLink/>
                    </a>
                </div>
                ))
            }
        </div>
    )

}

<Learning />
