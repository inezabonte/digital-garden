# Design 🎨

A collection of design related resources I've come across and use from time to time.

import {getAllDesignBookmarks} from "../../lib/api.js"
import { useSSG } from "nextra/ssg";
import {HiExternalLink} from 'react-icons/hi'

export const getStaticProps = async ({params}) => {
    const data = await getAllDesignBookmarks()
    return {
        props: {
            ssg: {
                data
            }
        }
    }

}

export const Design = () => {
    const {data} = useSSG()
    return (
        <div>
            {
                data.map((item) => (
                <div>
                    <h3>{item.name}</h3>
                    <span className="text-gray-600 dark:text-gray-400">{item.description}</span>
                    <a href={item.link} className="flex items-center" target="_blank">
                        <span>Link</span> 
                        <HiExternalLink/>
                    </a>
                </div>
                ))
            }
        </div>
    )

}

<Design />
