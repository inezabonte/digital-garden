# Today I Learned 📝

import { getAllTils } from "../lib/api.js";
import { useSSG } from "nextra/ssg";
import { MDXRemote } from "next-mdx-remote";
import DateComponent from "../components/DateComponent.js";

export const getStaticProps = async ({ params }) => {
	const tils = await getAllTils();
	return {
		props: {
			ssg: {
				tils,
			},
		},
		revalidate: 60,
	};
};

export const Til = () => {
	const { tils } = useSSG();
	return (
		<div className="mt-10 space-y-24">
			{tils.map((item) => (
				<div key={item.date}>
					<DateComponent date={item.date} displayFormat="PPP" />
					<MDXRemote {...item.content} />
				</div>
			))}
		</div>
	);
};

This section of my website is dedicated to things I learn during my day. This is the perfect place
to post it in case I ever forget it.

<Til />
